/// <reference types="node" />
import { EventEmitter } from 'events';
declare class WebSocketPolyfill extends EventEmitter {
    private _ws;
    constructor(uri: string);
    send(msg: Buffer, cb: (err?: Error) => void): void;
    ping(): void;
    close(): void;
}
export = WebSocketPolyfill;
